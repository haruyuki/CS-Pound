language: python
cache: pip
python:
  - "3.6"
  - "3.7-dev"

env: PYTHONPATH=$TRAVIS_BUILD_DIR

os: linux

install: pip install -r requirements.txt

before_script: pytest

script: python verify.py

after_script: pycodestyle *.py cogs/
