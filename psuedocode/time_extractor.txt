BEGIN TIME_EXTRACTOR(time)
    IF hours AND minutes AND seconds NOT IN time THEN
        total_hours = 0
        total_minutes = 0
        total_seconds = 0
    ELSE IF hours IN time THEN
        total_hours = time[hours]
        IF minutes IN time:
            total_minutes = time[minutes]
            IF seconds IN time THEN
                total_seconds = time[seconds]
            END IF
        ELSE
            IF seconds IN time THEN
                total_seconds = time[seconds]
            END IF
        END IF
    ELSE
        IF minutes IN time THEN
            total_minutes = time[minutes]
            IF seconds IN time THEN
                total_seconds = time[seconds]
            END IF
        ELSE
            IF seconds IN time THEN
                total_seconds = time[seconds]
            END IF
        END IF
    END IF
    IF total_hours AND total_minutes AND total_seconds == 0 THEN
        final_total_seconds = 0
    ELSE
        final_total_seconds = (total_hours * 60 * 60) + (minutes_total * 60) + seconds_total
    END IF
    RETURN final_total_seconds, total_hours, total_minutes, total_seconds
END